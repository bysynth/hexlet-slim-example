<!doctype html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Edit User</title>
</head>
<body>

<h1>Edit user with id <?= $id ?></h1>

<form action="/users/<?= $id ?>" method="post">
    <input type="hidden" name="_METHOD" value="PATCH">
    <div>
        <input type="text" name="user[nickname]" id="nickname" value="<?= htmlspecialchars($userData['nickname'] ?? '') ?>">
        <label for="nickname">Nickname</label>
    </div>
    <?php if (isset($errors['nickname'])): ?>
        <div style="font-size: 12px; color: red;"><?= $errors['nickname'] ?></div>
    <?php endif ?>
    <div>
        <input type="text" name="user[email]" id="email" value="<?= htmlspecialchars($userData['email'] ?? '') ?>">
        <label for="email">Email</label>
    </div>
    <?php if (isset($errors['email'])): ?>
        <div style="font-size: 12px; color: red;"><?= $errors['email'] ?></div>
    <?php endif ?>
    <div>
        <input type="submit" value="Update">
    </div>
</form>

<p><a href="/users">Back to users</a></p>

</body>
</html>